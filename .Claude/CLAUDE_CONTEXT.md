# Project Context

The canonical project context lives at the repo root:

**`H:\HyperAgent-SDK\CLAUDE_CONTEXT.md`** — read that first.

This `.claude/` folder holds local Claude config (skills, settings) only.
